{
  "name": "apecoin",
  "license": "UNLICENSED",
  "scripts": {
    "cargo-b": "cargo build --target wasm32-unknown-unknown --release",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service data-nexus/grt_substream",
    "create-local": "graph create --node http://localhost:8020/ itsjerryokolo/apecoin",
    "remove-local": "graph remove --node http://localhost:8020/ itsjerryokolo/apecoin",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 itsjerryokolo/apecoin",
    "test": "graph test"
  },
  "dependencies": {
    "@bufbuild/buf": "^1.23.1",
    "@graphprotocol/graph-cli": "0.51.1",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
